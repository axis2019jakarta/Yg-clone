{
  "name": "youtube-music-clone", // Nama proyek Anda
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite", // <-- Perintah yang dijalankan saat 'npm run dev'
    "build": "vite build", // Perintah untuk membuat versi produksi
    "lint": "eslint . --ext js,jsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview" // Untuk melihat hasil build secara lokal
  },
  "dependencies": {
    // Daftar "bahan" utama aplikasi
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    // Daftar "bahan" untuk development (tidak ikut di versi final)
    "@types/react": "^18.2.66",
    "@types/react-dom": "^18.2.22",
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.4.19", // <- Dependensi Tailwind
    "eslint": "^8.57.0",
    "eslint-plugin-react": "^7.34.1",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-react-refresh": "^0.4.6",
    "postcss": "^8.4.38",     // <- Dependensi Tailwind
    "tailwindcss": "^3.4.3", // <- Dependensi Tailwind
    "vite": "^5.2.0"
  }
}
